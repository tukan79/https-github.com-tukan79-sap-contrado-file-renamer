# Power Automate Desktop Flow

Place the reviewed Power Automate Desktop export package here.

Before upload, inspect it for real paths, email addresses, usernames, credentials, company data, customer data, screenshots, and environment-specific identifiers.

The repository excludes `.zip` files by default. Remove that rule only after a manual security review.
